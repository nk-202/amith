{
    "name": "render-root-fix",
    "version": "1.0.0",
    "description": "Proxy package to fix Render Root Directory misconfiguration",
    "scripts": {
        "build": "cd .. && npm install && npm run build && mkdir -p src/dist && cp -r dist/* src/dist"
    }
}